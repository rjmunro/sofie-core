$defs:
  quickLoopMarkerType:
    type: string
    title: QuickLoopMarkerType
    description: The type of entity the marker is locked to
    enum:
      - playlist
      - rundown
      - segment
      - part
